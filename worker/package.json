{
  "name": "vespo-quote-api",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that emails Vespo contact-quiz submissions to the owner.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
